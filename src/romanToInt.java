public class romanToInt {
    public int romanToInt(String s) {
        //if the roman value is a single digit string, return its value
        if(s.length() == 1) {

        }
        //else iterate through the String to determine the values
        for(int i = 0; i < s.length(); i++) {

        }
        return 0;
    }
}